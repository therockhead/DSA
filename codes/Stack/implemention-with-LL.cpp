// Stack Implementation with LinkedList
#include <bits/stdc++.h>
#define ll long long
using namespace std;
class Node{
    int data;
    Node* next;

    public:
    Node(int data) {
        this->data = data;
        this->next = nullptr;
    }
    
};
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    return 0;
}